int main() {
    local var int n = 0;
    local var int i = 1;
    local var int sum = 0;

    threadln("Enter N:");
    trap(n);

    while (i <= n) {
        sum = sum + (i * i * i);
        i = i + 1;
    }

    thread("Sum of cubes: ");
    threadln(sum);

    return 0;
}
