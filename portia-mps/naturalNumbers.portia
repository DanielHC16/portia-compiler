func int printNumbers(int n) {
    if (n > 50) {
        return 0;
    }

    thread(n);
    thread("  ");

    printNumbers(n + 1);

    return 0;
}

int main() {
    local var int start = 1;

    thread("The natural numbers are : ");
    threadln("");

    printNumbers(start);

    return 0;
}

