int main() {
    local var int num1 = 0;
    local var int num2 = 0;
    local var int prod = 0;

    threadln("Enter first number:");
    trap(num1);
    threadln("Enter second number:");
    trap(num2);

    prod = num1 * num2;

    thread("Product: ");
    threadln(prod);

    return 0;
}
